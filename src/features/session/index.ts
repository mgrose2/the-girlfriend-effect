// Name-only identity. No passwords, no tokens — see CLAUDE.md.
export { SessionProvider, useRequiredUser, useSession } from './SessionProvider';
export type { SessionValue } from './SessionProvider';
